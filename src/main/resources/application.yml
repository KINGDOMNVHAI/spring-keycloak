server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/junbi_dev?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/demo-realm

keycloak:
  realm: demo-realm
  auth-server-url: http://localhost:8080
  client-id: demo-client
